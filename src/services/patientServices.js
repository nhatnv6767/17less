import db from "../models/index";
import _ from "lodash";

let postBookAppointment = () => {
    return new Promise(async (resolve, reject) => {
        try {


        } catch (e) {
            reject(e);
        }
    });
};

module.exports = {
    postBookAppointment: postBookAppointment,
};
